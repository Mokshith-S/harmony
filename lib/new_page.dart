import 'package:flutter/material.dart';

class TempRoute extends StatelessWidget {
  const TempRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Content')),
      body: const Text('dfdf'),
    );
  }
}
